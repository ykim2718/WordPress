# WordPress

## Git workflow

Push directly to `main`. This is the repository owner's standing instruction.

- Do not open a pull request unless explicitly asked for one.
- If the session assigns a `claude/*` working branch, commit there as instructed,
  then push the commit to `main` as well (`git push origin <branch>:main`).
